---
- name: Ensure that systemd is reloaded
  become: true
  ansible.builtin.systemd:
    daemon_reload: true
  listen: restart service

- name: Run the ansible-pull systemd unit
  become: true
  ansible.builtin.systemd:
    name: ansible-pull.service
    state: restarted
  listen: restart service
